# VAPI Python example

Single example: **main.py** — window with Worksheet (canvas with blocks).

## Run

From repo root:

```cmd
set PATH=%CD%\bin;%PATH%
set PYTHONPATH=%CD%\bin
python examples/main.py
```

Or run the module (same PATH and PYTHONPATH):

```cmd
python -m vapi
```

See root **README.md** for requirements and API.
